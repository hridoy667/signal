/**
 * Server Actions for auth (e.g. cookie-based sessions). Most auth calls today
 * live in `@/services/auth.service.ts` on the client. Add `"use server"` files
 * or function bodies here when you introduce server actions.
 */

export {};
